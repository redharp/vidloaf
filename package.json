{
  "name": "vidloaf",
  "private": true,
  "scripts": {
    "backend:build": "yarn workspace backend run build",
    "backend:start": "yarn backend:build && cd dist && node server.js",
    "web:build": "yarn workspace web run build",
    "start": "yarn web:build && yarn backend:start"
  },
  "workspaces": [
    "packages/web",
    "packages/backend"
  ]
}
